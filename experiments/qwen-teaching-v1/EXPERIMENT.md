# Historical Qwen teaching experiment

Completed source code is preserved at commit `3619102f79b0c5fbe33ce5daa067c158a65dc184`. This folder preserves its dataset, configuration, scores and receipts. The synthetic SOP and Qwen model do not meet the subsequently supplied LLaMA/source requirements. These scores must not be reused for the replacement corpus.

The verified Qwen adapter remains in GitHub prerelease `v0.1.0-capstone`. Local binary artifacts moved to `artifacts/qwen-teaching-v1/`. Reproduce the earlier experiment from its recorded commit in a separate checkout, not with the replacement pipeline.
